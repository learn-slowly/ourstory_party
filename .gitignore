# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# seed source data (행안부 법정동코드 원본 — 재배포 부담)
data/seed/legaldong-source.txt

# data.go.kr raw 캐시 (재실행 시 재생성)
/data/raw/

# 투표소 파싱 결과 JSON (재실행 시 재생성)
data/raw/polling-stations/
data/processed/polling-stations/

# Claude Code 워크트리·로컬 설정
/.claude/

# 정적 빌드 산출물 (재실행 시 재생성)
# data/parsed/ 는 Vercel 빌드용으로 git 에 포함 — raw xlsx 부재 환경 대응
public/data/static/
